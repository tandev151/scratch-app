// import React from 'react';
import { Typography } from 'antd';

const LoginPage = () => {
  const { Title } = Typography;
  return (
    <div className="w-full flex justify-between bg-purple-950">
      <Title>BAO Insurance</Title>
    </div>
  );
};

export default LoginPage;
